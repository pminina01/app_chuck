name: networking_examples
description: Examples of networking

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.4

  json_annotation: ^4.4.0
  dio: ^4.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

  build_runner: ^2.1.7
  json_serializable: ^6.1.4

flutter:
  uses-material-design: true
